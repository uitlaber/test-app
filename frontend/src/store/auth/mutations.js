export function SET_TOKEN(state, token) {
    state.token = token
}

export function SET_USER(state, user) {
    state.user = user
}

export function SET_LOADED(state, loaded) {
    state.loaded = loaded
}